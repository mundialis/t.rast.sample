name: GRASS GIS addon manual

on:
  push:
    branches: [ main ]

permissions: {}

jobs:
  grass-manual:
    uses: mundialis/github-workflows/.github/workflows/grass-manual.yml@main
    permissions:
      contents: read
      pages: write
      id-token: write
